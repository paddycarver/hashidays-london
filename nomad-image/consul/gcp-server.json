{
	"datacenter": "{CLOUD}-{REGION}",
	"data_dir": "/opt/consul",
	"server": true,
	"bootstrap_expect": 3,
	"node_meta": {
		"cloud": "{CLOUD}",
		"instance_type": "{INSTANCE-TYPE}"
	},
	"retry_join_gce": {
		"tag_value": "consul-server",
		"zone_pattern": "{REGION}-.*"
	}
}
